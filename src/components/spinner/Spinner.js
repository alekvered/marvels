import Spin from './Spinner.gif';

const Spinner = () => {
  return (
    <img
      src={Spin}
      style={{ margin: '0 auto', background: 'none', display: 'block' }}
      alt="Spinner"
    />
  );
};

export default Spinner;
